# Quarantined image orphans

Moved here during the performance sweep so they do not ship to GitHub Pages
(`deploy-pages.yml` strips `images/_pipeline`).

Includes:

- Unused morph/lockup masters never referenced by HTML
- Superseded PNG/JPEG sources replaced by display-sized WebP in `images/product/` and `images/photos/`

Restore individually if a page needs the original raster again.
